{
  "name": "jeevanlink",
  "description": "Jeevan Link - Link Shortener Project",
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist --force-local",
    "build:frontend": "lerna run build --scope frontend-nextjs",
    "clean": "lerna run clean",
    "dev": "lerna run --parallel dev",
    "frontend": "lerna run dev --scope frontend-nextjs",
    "backend": "lerna run dev --scope backend-nodejs"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
